<?php

declare(strict_types=1);

// Existing untranslated strings, if any, are explicitly listed here so CI can
// prevent the set from ever growing. New development must add Arabic defaults
// rather than expanding this baseline.
return [];
