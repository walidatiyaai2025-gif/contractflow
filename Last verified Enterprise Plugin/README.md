# Latest verified Enterprise Safe Contracts plugin

This folder belongs only to the `enterprise-safecontracts` product line.

Stable retained filename: `EnterpriseSafeContracts-latest.zip`.

A retained binary must have a matching `.sha256` sidecar and `VERIFIED.json` provenance generated by `scripts/enterprise_verified_artifacts.py`. Keep at most one current verified plugin binary here; historical ESC releases belong in GitHub Releases.

Never place Safe Contract client artifacts in this folder, and never copy ESC artifacts into `Last verified Plugin/`.
