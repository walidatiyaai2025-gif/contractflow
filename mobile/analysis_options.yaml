include: package:flutter_lints/flutter.yaml

analyzer:
  language:
    strict-casts: true
    strict-inference: true
    strict-raw-types: true

linter:
  rules:
    always_declare_return_types: true
    avoid_print: true
    prefer_final_locals: true
    # Compound ValueKey identifiers keep braces around adjacent interpolations
    # so each identifier component stays visually unambiguous.
    unnecessary_brace_in_string_interps: false
